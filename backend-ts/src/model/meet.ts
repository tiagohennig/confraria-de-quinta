export interface MeetInputDTO {
    id: string;
    name: string;
    date: string;
    location: string;
}

export interface MeetUpdateDTO {
    id: string;
    name: string;
    date: string;
    location: string;
}

export interface MeetInput {
    name: string;
    date: string;
    location: string;
}